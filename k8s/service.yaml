apiVersion: v1
kind: Service
metadata:
  name: pwgen-api
  labels:
    app.kubernetes.io/name: pwgen-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: pwgen-api
  ports:
    - name: http
      port: 80
      targetPort: http # -> containerPort 8080
      protocol: TCP
